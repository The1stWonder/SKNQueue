<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    x:Class="MasterQ.BranchChooseServiceQueuePage"
    NavigationPage.HasNavigationBar="False" 
    BackgroundColor="White">

    <Grid Margin="25,0,25,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="100*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition  Width="60*" />
            <ColumnDefinition  Width="40*" />
        </Grid.ColumnDefinitions>
        <!--<StackLayout Grid.Row="0" Grid.Column="0" VerticalOptions="FillAndExpand" HorizontalOptions="Fill">
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="Center">
                <ContentView Padding="0,60,0,0" VerticalOptions="FillAndExpand">
                    <Image Source="Qcircle.png" VerticalOptions="Center" HeightRequest="400" />
                </ContentView>
            </StackLayout>
        </StackLayout>-->
        <Image Grid.Row="0" Grid.Column="0" Source="circleQiPad.png" VerticalOptions="Center" HeightRequest="400" />
        <Label x:Name="NumberQ" Grid.Row="0" Grid.Column="0" FontSize="100" Text="-" TextColor="White" VerticalOptions="Center" HorizontalOptions="Center" />
        
        <StackLayout Grid.Row="0" Grid.Column="1" VerticalOptions="FillAndExpand" HorizontalOptions="Fill">
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="Center">
                <ContentView Padding="10,40,0,40" VerticalOptions="FillAndExpand">
                <ListView
                    ItemTapped="itemTapped"
                    BackgroundColor="Transparent"
                    RowHeight="75"
                    SeparatorVisibility="None"
                    x:Name="ServiceListview"
                    VerticalOptions="Fill"
                    HorizontalOptions="Fill">
                <ListView.ItemTemplate>
                <DataTemplate>
                <ViewCell>
                <StackLayout Orientation="Vertical">
                <StackLayout Orientation="Horizontal">
                <Label Text="{Binding serviceName}" TextColor="Black" />
                    <Image HorizontalOptions="EndAndExpand" Source="NextQ2.png" HeightRequest="40"/>
                </StackLayout>
                </StackLayout>
                </ViewCell>             
                </DataTemplate>
                </ListView.ItemTemplate>
                </ListView>
                </ContentView>
            </StackLayout>
        </StackLayout>
    </Grid>
</ContentPage>
